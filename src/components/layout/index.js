import React from 'react'

import Header from 'components/header'

export default function Layout (props) {
    return (
        <React.Fragment>
            <Header />
            <p>this is Layout</p>
        </React.Fragment>
    )
}